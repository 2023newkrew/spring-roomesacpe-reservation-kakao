spring:
  h2:
    console:
      enabled: true
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:~/roomescape
    username: kakao
    password:
  sql:
    init:
      mode: always
      schema-locations: classpath:schema/reservation_schema.sql, classpath:schema/theme_schema.sql
      data-locations: classpath:data/reservation_init.sql, classpath:data/theme_init.sql